[project]
name = "local-llama-claudecode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hf-transfer>=0.1.9",
    "huggingface-hub>=1.6.0",
]
